{
  "name": "universal-mod-launcher",
  "version": "1.0.0",
  "description": "Universal Mod Launcher (UML) em Electron",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . UML --platform=win32 --arch=x64 --out=dist --overwrite"
  },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "fs-extra": "^11.3.0",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "electron": "^37.3.0"
  }
}
